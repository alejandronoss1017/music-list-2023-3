export const environment = {
  production: false,
  apiKey: 'exampleKey',
};

// Angular takes care of swapping the environment file for the correct one.
// To run the app in development mode (environment.ts), use this command:
// ng serve
// To run the app in production mode (environment.prod.ts), use this command:
// ng serve --configuration=production
